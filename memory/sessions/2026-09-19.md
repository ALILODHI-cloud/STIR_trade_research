# 2026-09-19 — setup

First session. No trades discussed yet.

## What was established

- Scope: research agent for a DM STIR book. Evaluate trade ideas using public
  rates/econ data plus sell-side PDFs the owner supplies.
- Owner is new to working with a research agent — explain mechanics plainly.
- Owner requires complete recall of everything discussed. Hence `memory/`.

## Environment facts (verified, not assumed)

- Egress policy blocks FRED, CME, NY Fed, ECB, BoE — all 403 at the proxy.
  Tested by curl and by the agent's own fetch tool. Web *search* works; web
  *fetch* does not. So data must come from disk or from pastes.
- PyPI and npm are reachable, so tooling can be installed.
- Push to the repo is BLOCKED: the Claude GitHub App is not installed on
  ALILODHI-cloud/STIR_trade_research. Until fixed, nothing here is durable.
  Remedy: install at https://github.com/apps/claude/installations/select_target
  or reconnect GitHub from claude.ai connector settings.

## Built

- `lib/stir/` — conventions, futures, ois, rv, data. 42 tests.
- `memory/` — this directory, plus a SessionStart hook that prints it into
  context at the start of every session.
- ruff configured; lint clean.

## Two bugs found by the tests, worth remembering as a caution

- PCA residual omitted the column-mean add-back, so cumulated residuals grew a
  spurious trend — i.e. it manufactured a rich/cheap signal from nothing.
- The OIS bootstrap mis-attributed under-determined quotes; it now splits them
  evenly and labels that a convention.

Both were silent, plausible-looking wrong answers. This is the argument for
putting the arithmetic in tested code rather than in prose.

## Resolved later the same day

- GitHub App installed. Push succeeded; remote and local at 291c2fa. The
  memory system is now actually durable rather than theoretically so.
- Hook verified end to end: it fired on a session resume and loaded book.md,
  trades.yaml and this log into context.

## Still open

- Fly sign convention unconfirmed against the owner's desk. ASK BEFORE
  QUOTING ANY FLY NUMBER.
- Position-tracking scope undecided (ideas only vs full live book; fills/P&L).
